export interface IUsersDashboardData {
    active_users: number,
    all_users: number,
    inactive_users: number,
}

export interface IBonusesDashboardData {
    given_out: number,
    expected_for_release: number,
    used_bonus: number,
}